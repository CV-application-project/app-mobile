import {
    StyleSheet
} from 'react-native';
const StyleProfile = StyleSheet.create({
    container: {
        marginBottom: 12
    },

    avatar : {
     
    },

    form : {

    }
   
});
export default StyleProfile;
